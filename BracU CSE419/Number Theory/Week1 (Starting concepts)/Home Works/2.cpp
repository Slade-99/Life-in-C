/*

#include <bits/stdc++.h>
using namespace std;

int main() {
  int i = 1;
  cin.tie(0);
  cout.tie(0);

  int T;
  std::cin >> T;
  while (T--) {
    int a, b, c;
    std::cin >> a >> b >> c;
    if (a <= b && a <= c) {
      if (b <= c) {
        cout << "Case " << i << ": " << b << endl;
      } else {
        cout << "Case " << i << ": " << c << endl;
      }
    } else if (b <= a && b <= c) {
      if (a <= c) {
        cout << "Case " << i << ": " << a << endl;

      } else {
        cout << "Case " << i << ": " << c << endl;
      }
    } else {
      if (a <= b) {
        cout << "Case " << i << ": " << a << endl;
      } else {
        cout << "Case " << i << ": " << b << endl;
      }
    }
    i++;
  }

  return 0;
}

*/